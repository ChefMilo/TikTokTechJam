"""Harness package: data loading, splits, metrics, noise gate, and caching
for the KuaiRand recommender-system benchmark.

Owned by the harness team. Other packages should treat this as a stable
API surface (see contracts.py) rather than reaching into internals.
"""
