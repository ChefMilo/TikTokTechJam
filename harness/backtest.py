"""Backtesting harness that runs a method over historical data/splits.

Will hold: the rung/backtest loop that drives a candidate method through
progressively larger data slices and records results.
"""


def run_backtest(*args, **kwargs):
    pass
